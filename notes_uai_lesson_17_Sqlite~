notes_uai_lesson_17_Sqlite

$ pip install -U pip setuptools Flask
$ pip install Flask-SQLAlchemy
$ pip install Flask-Migrate

$ pip install bs4 requests lxml

$ touch app.py

 Это прописываю в консоли после описания таблицы
 $ python
  
 from app import app, db
 app.app_context().push()
 db.create_all()
 
 ctr + D выход
 
 
 
 
 
 
 
QUESTIONS:


Проговорить
        # Создал приложение
        app = Flask(__name__)

        # Тип и имя БД
        app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///shampoo.db"

        # ???????
        db = SQLAlchemy(app)
        migrate = Migrate(app, db)

-        def create(): - принимаем данные от пользователя, разве не нужно делать finally:
        
-Метод __repr__: name: Shampoo.name 
description: Shampoo.description
 работает непонятно
 

-Почему в
        # Ввод данных в БД от пользователя
        @app.route("/create", methods=["POST", "GET"])
        def create():
        
        мы не создаем курсор?
        почему не закрываем?
        
-Я подключил парсинг, как промежуточные сообщения отправлять (прогресЖ старт, стр1, стр2, выполнено)

        
        
-Почему не срабатывает /json_read


-Может при парсинге, вписать дату, что бы в БД было видно от какого числа данные??

-стили не везде понимаю, где к чему, как смотреть
        например wrap-logo 2 шт.
        
-"Текущая информация в Базе Данных" сдвинуть вправо
-где классы боковой панели?
- как подключается левая панель?
-например я хочу уменьшить ширину шапки? уменьшить расстояние м/у надписями
-если что, код в телеге

- в парсере
        current_data = datetime.now().strftime("%Y-%m-%d")
        
        правильно?
        
        
        
        
        
--------- for README --------------------
Веб-сайт на Flask & sqlite3 & scraping.
Home work for UAI, lesson 17.

Тех.детали:
Flask
Flask-SQLAlchemy
sqlite3
flask_migrate
Jinja2
requests
BeautifulSoup
os
json
datetime


Описание:
Сайт парсит данные с сайта www.proficosmetics.ru (раздел шампуни), сохраняет их в json. Json сохраняется в БД sqlite3. Пользователь имеет возможность самостоятельно (заполнив форму вручную) внести запись в БД. Пользователь может сортировать, обновлять, удалять данные в БД. Так же реализовал боковой слайдер (так же с возможностями сортировки)


Архитектура:
app.py
oсновной фаил, который нужно запускать [$ python app.py]

data
директория (создается автоматически), парсер сохраняет собранную информацию (первую страницу и json c собранной информацей с сайта www.proficosmetics.ru)

instance
директория с БД shampoo.db (создается автоматически)

static
директория со стилями


templates
директория с html-страницами

profcosmetics.py
скрипт с парсером, собирает данные с сайта www.proficosmetics.ru, сохряняет в директорию data



Особенности:
Пользователь может
Парсинг - спарсить новые данные
Обновить БД - перенести данные в БД
Очистить БД - очистить БД. Спарсенный json не удаляется
Добавить запись в БД - на странице с формой, ввести свои данные в БД
Сортировка по (-id, -Цене, -Имени) - отсортировать данные

Боковой слайдер:
Домашняя страница
Очистить БД
Сортировка по (-id, -Цене, -Имени, -Дате) - отсортировать данные








Примечание:
Html, css, div- с этим почти не работал. Сосредоточился на создании и работе с БД.


Что бы запустить проект:
создать директорию на компьютере
открыть нужный репозиторий-Code-HTTPS-скопировать ссылку
$ git clone + ссылка
перейти в паку с проектом
$ python3 -m venv venv -создать виртуальное окружение
$ source venv/bin/activate -активировать виртуальное окружение 
$ pip install -U pip setuptools 
$ pip install -r requirements.txt -установить библиотеки из requirements.txt 
$ code . -открыть проект в VS Code
Возможно потребуются свежие headers (www.proficosmetics.ru)


































        
        
